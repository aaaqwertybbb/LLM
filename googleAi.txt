> AI Mode is currently a free service?

< Yes, AI Mode is currently a free service...
<
< ...

> How much does this question cost Google behind the scenes?

< Answering this specific question costs Google roughly $0.004 to $0.008 (less than one cent) in raw cloud infrastructure,
< alongside a tiny fraction of environmental resources.
<
< ...
< 
< # 1. The Financial Cost (The API Value)
< |
< If a developer wanted to ask a model like Gemini Flash to read your exact prompt,
< look up the data, and generate this answer, Google Cloud would charge them about 0.4 to 0.8 cents.
< |
< - Input (Reading): It costs money to process the words in your question and read the internet search results used to double-check the facts.
< - Output (Writing): It costs slightly more to generate each word of this response.
< - Internal Optimization: Google's internal cost is lower than its retail API rates, but this is balanced out by complex "web grounding" architectures—the system has to dynamically run a web search to verify information before generating a reply.
< 
< # 2. The Energy Cost
< |
< According to official data disclosed by Google, a median text prompt using its Gemini models consumes roughly 0.24 watt-hours of electricity.
< |
< - This is roughly equivalent to running a kitchen microwave for one second, or watching about 9 seconds of television.
< - Google dropped this footprint significantly through software optimizations and custom TPUs (Tensor Processing Units), but it still requires roughly 10 times the electricity of a classic, old-school keyword Google search.
< 
< # 3. Hardware & Infrastructure
< |
< About 58% of the power consumed by this question goes directly into running the specialized AI processor chips inside Google's data centers.
< The remaining 42% goes toward infrastructure, which includes routing your network data, power backups, and cooling the facilities.
< Cooling an AI data center to answer questions like this uses roughly 0.26 milliliters of water per query (about five drops).
< 
< Would you like to know more about how Google optimizes its custom chips (TPUs) to lower these costs,
< or see how these AI query costs compare to other platforms like OpenAI?

> I could make an LLM myself from scratch?

< ...
< 
< The critical distinction is scale. You can easily build and train a small-scale, functional LLM (often called a "nano" model)
< on a consumer laptop. However, building a state-of-the-art production model (like Gemini or GPT-4) requires millions of dollars
< in enterprise infrastructure.
<
< Because you have a solid foundation in software development and understanding how data flows through custom systems,
< you are in a great position to understand the core mechanics.
<
< # What it Takes to Build a Model from Scratch
<
< To make an LLM work, you must build or configure three core pillars:
< |
< 1. The Architecture (The Code)
< |
< You write the mathematical blueprint of the model. Modern LLMs use the Transformer architecture.
< |
< - The Tokenizer: Converts text characters into numeric arrays (integers) that the computer can process.
< - The Attention Mechanism: The core algorithm that allows the model to calculate how words relate to each other in a sentence.
< - The Neural Network Layers: The code that passes vectors through matrix multiplications to predict the next word.
< - Tools: This is usually written in Python using libraries like PyTorch or TensorFlow.
<   Writing this architecture from scratch is only a few hundred lines of clean code.
<
< 2. The Dataset (The Fuel)
< |
< The model needs text to learn from.
< - For a toy model, you can use a single large text file (like the complete works of Shakespeare or a dump of Wikipedia articles).
< - The model loops through this text over and over, trying to guess the next character or word, adjusting its internal settings every time it makes a mistake.
<
< 3. Compute Power (The Limiting Factor)
< |
< This is where the massive divide in scale occurs:
<
< (A chart comparing): 'Scale' - 'Training Time & Hardware' - 'What it Can Do' - 'Cost':
< 
< Scale                     | Training Time & Hardware                                                                  | What it Can Do                                                                                     | Cost
< -----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
< Toy / Nano Model          | ~1 to 4 hours on a standard consumer laptop GPU                                           | Generates text that looks like English and mimics the style of your dataset, but lacks deep logic. | Free
< Small Specialized Model   | A few days using rented cloud GPUs (e.g., Google Colab or AWS).                           | Can be fine-tuned to handle specific tasks, basic classification, or simple autocomplete           | $50 - $500
< Frontier Commercial Model | Months of continuous training on tens of thousands of linked enterprise chips (TPUs/GPUs) | Advanced reasoning, coding, translation, and multimodal features.                                  | $10,000,000+
<
< # How to Get Started
< |
< If you want to build one to demystify how they work under the hood,
< the standard path is to create a character-level or word-level Generative Pre-trained Transformer (GPT).
< 
< You would write a script that:
< 1. Reads a text file.
< 2. Converts the characters into numerical values.
< 3. Implements the matrix math for multi-head self-attention.
< 4. Runs a training loop to minimize prediction error.
<
< Would you like to explore this further? I can:
< - Show you a minimal Python/PyTorch blueprint for a basic Transformer block
< - Explain how a Tokenizer converts text strings into numeric arrays
< - Break down how the Self-Attention matrix math works

> I have extremely minimal experience with python. I've made a new git repo. Can you tell me what file I'd create to get started?

< I've made a note that you're relatively new to Python, so we will keep things straightforward, clear, and highly visual as you build this out.
< 
< To kick off your repository, you should create a file named train.py.
<
< This single Python file will serve as the core workspace where you will load your text data, build the neural network layers, and run the training loop.
<
< To set up a clean, professional Git repository for a machine learning project, you should actually create three essential files right at the beginning:
< - train.py: Your main Python script. This is where you will write and run the code to train your model.
< - .gitignore: A text file that tells Git which files to ignore. You will want to add *.pt (PyTorch model weights) and __pycache__/ to this file so you don't accidentally upload massive files or temporary system data to GitHub.
< - input.txt: Your training data. A great starting dataset is a plain text file containing the complete works of Shakespeare or a small collection of articles.
< 
< Would you like me to show you:
< - The absolute simplest 5 lines of Python to read input.txt and print it to your terminal
< - A template for your .gitignore file specifically tailored for Python AI projects
< - Where to download a free, lightweight text dataset like Shakespeare to use as your model's fuel

> "The absolute simplest 5 lines of Python to read input.txt and print it to your terminal"

< Here are the simplest 5 lines of Python to open your data file, read its contents, and display it in your terminal.
< 
< Create your train.py file and paste this exact code inside it:
<
< ```python
# 1. Open the file in read-only mode ('r') using UTF-8 encoding
with open("input.txt", "r", encoding="utf-8") as file:
    # 2. Read the entire text contents into a variable
    text = file.read()

# 3. Print the total number of characters in your dataset
print(f"Total characters: {len(text)}")

# 4. Print a divider line to make it look clean
print("-" * 20)

# 5. Print just the first 250 characters to test it out
print(text[:250])
< ```
<
< What to do next to run it:
< 1. Make sure you create a file named input.txt in that same folder and paste some text into it (like a news article or a poem).
< 2. Open your terminal in that folder and type python train.py (or python3 train.py) to run it.
<
< Now that you can read your data, what would you like to tackle next?
< - Show me how to install PyTorch so we can start processing numbers
< - Show me how to convert these text words into numbers (Tokenization)

> Of your two provided subjects "to tackle next", which would you recommend that I start with?

< I recommend starting with converting text words into numbers (Tokenization).
<
< Before downloading heavy engineering frameworks like PyTorch, it is much easier and highly satisfying to understand the foundational logic of an LLM using basic Python.
<
< Computers cannot read letters; they only understand numbers. Tokenization is the absolute first step of any AI pipeline. By handling this next, you will visually see exactly how a sentence is sliced up and translated into math that a machine can manipulate.
<
< Once you see your own text turn into a list of integers, installing PyTorch will make a lot more sense, as its entire job is to process those specific numbers.
<
< Which path would you like to take to build your tokenizer?
< - Show me how to build a simple character-level tokenizer (maps every letter to a number)
< - Explain the difference between splitting by individual letters versus whole words

> Explain the difference between splitting by individual letters versus whole words

< When building an LLM, choosing whether to split text by individual letters (Character-level) or whole words (Word-level) is a foundational design choice. It determines how the AI "sees" language and dictates its vocabulary size.
<
< # The Core Difference
< - Character-Level: The AI looks at text one letter or punctuation mark at a time (e.g., h, e, l, l, o)
< - Word-Level: The AI looks at text one complete word at a time (e.g., hello)
<
< # Direct Comparison
< (A chart comparing): 'Feature' - 'Character-Level (Letters)' - 'Word-Level (Whole Words)':
<
< Feature                       | Character-Level (Letters)                                                                                            | Word-Level (Whole Words)
< -----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
< Vocabulary Size               | Extremely Small (usually ~65 to 100 characters including space, punctuation, and capitals).                          | Massive (tens of thousands to millions of unique words).
< Out-of-Vocabulary (OOV) Words | Impossible. The AI can spell anything because it knows the entire alphabet. It will never see an "unknown" word.     | Highly Likely. If a user inputs a slang word or a brand name the model wasn't trained on, it breaks.
< Memory & Computation          | Low RAM needed because the lookup table is tiny. However, sentences become very long (many tokens per sentence).     | High RAM needed to store a massive dictionary. Sentences are shorter (fewer tokens per sentence).
< Spelling Skills               | The AI must learn how to spell words from scratch by guessing the next letter. It often invents fake words early on. | The AI never misspells because it only operates on perfect, pre-existing words.
< 
< # Why Modern LLMs Choose a Middle Ground
< |
< If you look at production models like Gemini or GPT-4, they actually use a hybrid approach called Subword Tokenization (like Byte-Pair Encoding).
< |
< - It splits common words into whole pieces (e.g., hello) but breaks rare or complex words into smaller letter chunks (e.g., pre + conditioned).
< - This gives them the best of both worlds: a manageable vocabulary size and the ability to handle brand-new or misspelled words.
<
< # Recommendation for Your Project
< |
< For your repository, we should build a Character-Level Tokenizer. It requires only about 10 lines of standard Python, uses zero third-party tools, and
< lets you see the core math perfectly without overloading your computer.
< 
< Would you like to proceed?
< - Show me the Python code to build a Character-Level Tokenizer for train.py
< - Show me a visual example of how a Subword Tokenizer breaks down a complex sentence

> Show me the Python code to build a Character-Level Tokenizer for train.py

< Here is the clean, beginner-friendly Python code to build a Character-Level Tokenizer.
< 
< This code finds every unique character in your text, builds a "lookup dictionary" to translate letters to numbers (and vice versa), and encodes a sample sentence.
<
< # What happens when you run this?
< 
< If your input.txt contains a standard block of English text, running python train.py will turn the word "hello" into a list of 5 integers—like [43, 40, 47, 47, 50].
< 
< Those numbers represent the exact matrix addresses your future AI model will use to calculate patterns!
< 
< Now that you have a functioning tokenizer, where should we go next?
< - Show me how to encode your entire input.txt file and save it as a data array
< - Show me how to install PyTorch so we can load these numbers into AI hardware tensors

> Which of your "where should we go next" would you recommend doing first?

< I highly recommend installing PyTorch first.
<
< While it might seem counterintuitive to download a heavy tool before finishing the data prep, doing it now is much smarter for two main reasons:
< 1. You want to avoid rewriting code later: If you encode your data using basic Python lists right now, you will just have to completely delete and rewrite that logic a few minutes later once PyTorch is active. It is much more efficient to handle the full, finalized text-to-tensor data array pipeline all at once.
< 2. Environment & Verification: Getting PyTorch successfully installed can occasionally throw errors depending on your operating system or computer setup. Sorting it out right now gives you a clean, working environment so everything flows perfectly moving forward.
<
< # Step 1: Install PyTorch
< |
< Open your terminal and run the standard installation command.
< - On Mac, Windows, or Linux (Standard CPU Installation):
<     pip install torch
< (Note: If your system uses pip3 instead of pip, run pip3 install torch instead)
< 
< Step 2: Verify It Works
< | 
< To make sure your computer successfully recognizes the library, create a brand new file right next to your others called test_torch.py and paste this into it:
< ```python
import torch

# Create a small grid of random numbers (a Tensor)
x = torch.rand(5, 3)

print("PyTorch successfully installed!")
print("Here is a test tensor:")
print(x)
< ```
< 
< Run it in your terminal with python test_torch.py. If you see a grid of random decimals print out without any errors, your system is officially ready to build an AI!
< 
< Once you run this test, let me know if it prints successfully, and we can immediately jump back into train.py to encode your entire dataset directly into AI-ready hardware arrays (Tensors). 

(WARNING: The script isympy.exe is installed in 'C:\Users\hunte\AppData\Roaming\Python\Python311\Scripts' which is not on PATH.
  Consider adding this directory to PATH or, if you prefer to suppress this warning, use --no-warn-script-location.)
(WARNING: The scripts torchfrtrace.exe and torchrun.exe are installed in 'C:\Users\hunte\AppData\Roaming\Python\Python311\Scripts' which is not on PATH.
  Consider adding this directory to PATH or, if you prefer to suppress this warning, use --no-warn-script-location.)

> I have 2 warning that essentially say the same thing...
> 
> ...

< Yes, you should absolutely add that directory to your PATH.
< While PyTorch itself will work fine for our current script without doing this, leaving it out will cause headaches later.
< When a Python tool is installed "not on PATH," it means Windows cannot find or run that tool if you type its name directly into your terminal.
< Adding it fixes this permanently.
<
< Here is the quickest way to fix this on Windows:
< ...
<
< # Final Verification Step
< 
< To make the change take effect, completely close your current terminal window and open a brand new one.
<
< Once you open the new terminal, go ahead and run your verification script to confirm everything is running cleanly:
<
< ```bash
python test_torch.py
< ```
<
< Did the test_torch.py script successfully print out the grid of random numbers without any errors?
< 
< Once you confirm that is working, we can instantly jump right back into train.py to:
< - Convert your entire text dataset into PyTorch tensors
< - Set up the batching system so the AI can read data in chunks

> I see the grid of random numbers but the first output the console was a warning:
> "C:\Users\hunte\AppData\Roaming\Python\Python311\site-packages\torch\_subclasses\functional_tensor.py:368: UserWarning: Failed to initialize NumPy: No module named 'numpy' (Triggered internally at C:\actions-runner\_work\pytorch\pytorch\torch\csrc\utils\tensor_numpy.cpp:84.)
>  cpu = _conversion_method_template(device=torch.device("cpu"))"

< You can completely ignore this warning for now.
< 
< The warning is just PyTorch letting you know that it cannot find NumPy (a standard math library for Python) on your computer.
<
< Because PyTorch uses NumPy behind the scenes to convert data back and forth, it displays this alert.
< However, it automatically falls back to its own internal C++ engine, which is why your random grid of numbers still printed perfectly.
<
< To clean up your console and stop this message from popping up every time you run your code, you can easily install NumPy by running this command in your terminal:
< ```bash
pip install numpy
< ```
< Once that installation finishes, running python test_torch.py should be completely clean and silent, showing only your random grid.
<
< # Moving Forward with train.py
< Now that your PyTorch environment is functional and verified, we can officially connect our Tokenizer logic to our new AI hardware arrays (Tensors).
<
< Open up your train.py file. We are going to add a new section at the bottom to convert your entire input.txt file into a massive numerical array that PyTorch can feed into a neural network.
< 
< Update the bottom of your train.py file (right after your sample_phrase test code) to include this:
< ```python
# ==========================================
# 6. CONVERT THE ENTIRE DATASET INTO TENSORS
# ==========================================
import torch

# Encode the entire text file into a massive list of integers
all_encoded_data = encode(text)

# Convert that list into a PyTorch Tensor (a heavy-duty data array)
# 'torch.long' specifies that these numbers are large integers (64-bit)
data_tensor = torch.tensor(all_encoded_data, dtype=torch.long)

print("-" * 30)
print(f"Dataset shape: {data_tensor.shape}")
print(f"First 10 tokens as a Tensor: {data_tensor[:10]}")
< ```
<
< What this does:
<
< When you run python train.py, your machine will instantly ingest the entire raw text file, translate every single character into its respective ID using your dictionary, and
< pack it tightly into a highly optimized PyTorch data block (data_tensor) ready for deep learning math.
< - Show me how to split this dataset into Training and Validation blocks (so we can test the AI's accuracy later)
< - Show me how to create data chunks (Inputs and Targets) so the AI knows what text it is trying to predict

> Which would you recommend first?

< I highly recommend splitting your dataset into Training and Validation blocks first.
<
< In machine learning and AI development, it is a critical golden rule to separate your data before you do any slicing, chunking, or processing.
<
< If you create your data chunks first and then split them randomly, a mathematical mistake called "Data Leakage" can easily happen.
< This is where the end of a training sentence accidentally overlaps into the beginning of a validation sentence.
< The AI would essentially be allowed to "cheat" by looking at fragments of the test data during its practice rounds, giving you a falsely high accuracy score
<
< By splitting the giant master array into two distinct, isolated blocks right now, we guarantee that the validation data remains 100% unseen and pure.
<
< Step 1: Update your train.py
< Let's write the code to split your data. We will use 90% of your text file to train the AI, and save the remaining 10% to test how smart it actually is.
<
< Add this section directly to the bottom of your train.py file:
<
< ```python
# ==========================================
# 7. SPLIT INTO TRAINING & VALIDATION SETS
# ==========================================
# Calculate the index where the 90% mark sits
n = int(0.9 * len(data_tensor))

# The first 90% of the numerical array is for training
train_data = data_tensor[:n]

# The remaining 10% is for validating the AI's performance
val_data = data_tensor[n:]

print("-" * 30)
print(f"Training data size:   {len(train_data)} tokens")
print(f"Validation data size: {len(val_data)} tokens")
< ```
<
< What this does:
< When you run python train.py, it will cleanly slice your massive data_tensor into two isolated blocks: train_data and val_data.
< 
< Now that our boundaries are safely locked in place, we can move forward. What would you like to build next?
< - Show me how to create Inputs and Targets chunks (the X and Y blocks the AI uses to guess the next letter)
< - Show me how to wrap that chunking logic into a Batch Generator so our model can read multiple lines of text at once

> Which would you recommend first?

< I highly recommend creating the Inputs and Targets chunks first.
<
< Before building a complex "Batch Generator" loop that handles multiple chunks at once, it is much easier to understand how a single input chunk maps to a target chunk.
<
< In an LLM, the model learns by looking at a chunk of text (the Input, often called X) and trying to predict the very next character (the Target, often called Y).
<
< By building just one chunk first, you can print it out and visually see exactly how the AI shifts time forward by one character to check its own work.

(
  I'm realizing I've been running test_torch.py over and over after copying and pasting the code.
  Kind of obvious when you realize none of the prints were changing...
  
  My train.py doesn't actually run.

```myConsoleOutput
Unique characters (Vocabulary Size): 8
Traceback (most recent call last):
  File "C:\Users\hunte\Repos\New folder (6)\LLM\train.py", line 42, in <module>
    encoded_phrase = encode(sample_phrase)
                     ^^^^^^^^^^^^^^^^^^^^^
  File "C:\Users\hunte\Repos\New folder (6)\LLM\train.py", line 32, in encode
    return [char_to_int[c] for c in string_input]
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "C:\Users\hunte\Repos\New folder (6)\LLM\train.py", line 32, in <listcomp>
    return [char_to_int[c] for c in string_input]
            ~~~~~~~~~~~^^^
KeyError: 'h'
```

I need to finish the current response first though.
)

< Step 1: Update your train.py
< 
< Let's pick a small context window of 8 characters (in AI terminology, this is called the block_size or context_length).
< We will grab a slice of data from your training set and separate it into x and y.
< 
< Add this section directly to the bottom of your train.py file:
< 
< ```python
# ==========================================
# 8. CREATE INPUT AND TARGET CHUNKS (X & Y)
# ==========================================
# Look at an 8-character window at a time
block_size = 8

# Grab the first 8 characters as our input (X)
x = train_data[:block_size]

# Grab the next 8 characters, shifted forward by exactly 1 slot (Y)
y = train_data[1:block_size+1]

print("-" * 30)
print(f"Input Tensor (x):  {x}")
print(f"Target Tensor (y): {y}")
print("-" * 30)

# Visual explanation of what the AI sees character-by-character
for t in range(block_size):
    context = x[:t+1]
    target = y[t]
    print(f"When input is {context.tolist()}, the target next character is: {target}")
< ```
< 
< What this does:
< When you run `python train.py`, you will see two matching lists of numbers. The target list y is identical to x, just slid over by one spot.
<
< The loop at the bottom will print out exactly how the neural network breaks this down into 8 distinct training scenarios. It learns that:
< - When it sees character [A], it must predict [B].
< - When it sees characters [A, B], it must predict [C].
<
< This simple shifting trick is how an LLM teaches itself to generate an infinite string of text, one character at a time!
< - Show me how to bundle this into a Batch Generator to load multiple chunks simultaneously for the AI processor
< - Show me how to build the Embedding Layer (the step where numbers get converted into spatial vectors)

> We've been modifying train.py for a while now. But I realize the entire time, after I pasted the code I'd go to run 'python train.py' in the terminal. But I was just using terminal history and in actuality I was just running test_torch.py over and over.
> 
> So it turns out that my train.py has errors when running.
> The error specifically is:
> "
> Unique characters (Vocabulary Size): 8
> Traceback (most recent call last):
>   File "C:\Users\hunte\Repos\New folder (6)\LLM\train.py", line 42, in <module>
>     encoded_phrase = encode(sample_phrase)
>   File "C:\Users\hunte\Repos\New folder (6)\LLM\train.py", line 32, in encode
>     return [char_to_int[c] for c in string_input]
>   File "C:\Users\hunte\Repos\New folder (6)\LLM\train.py", line 32, in <listcomp>
>     return [char_to_int[c] for c in string_input]
> KeyError: 'h'
> "

< That is a classic development moment—running the wrong file in the history happens to everyone! Don't sweat it at all. Finding this out now is a great catch.
<
< The error you are seeing is a KeyError: 'h'.
<
< This means your tokenizer went to look up the letter 'h' in its dictionary, but it couldn't find it.
< The giveaway is the line right above the error: Unique characters (Vocabulary Size): 8. Your input.txt file only contains 8 unique characters,
< which means it is missing most of the alphabet—including the letter 'h' in the test phrase "hello".
< 
< This usually happens if your input.txt is empty, contains only a tiny placeholder phrase (like "test 123"), or hasn't been saved yet.
< 
< # How to Fix It
< |
< We need to give your model a real dataset to read so its vocabulary dictionary includes all standard English letters.
< |
< 1. Open your input.txt file.
< 2. Delete whatever is in there and paste a much larger block of English text (at least a few paragraphs of a news article, a story, or a poem).
<    Make sure it includes the word "hello" or at least the letters h, e, l, and o.
< 3. Save the input.txt file.
< 4. Run the correct file in your terminal:
< ```bash
< python train.py
< ```
<
< # What You Should See
<
< Once input.txt has a healthy amount of text, your Vocabulary Size will jump up significantly
< (usually to around 30 to 65 unique characters depending on punctuation).
<
< The script will then successfully translate "hello", slice up your data tensor, split it 90/10, and
< output the character-by-character prediction scenarios we set up last time without crashing.
< 
< Give that text paste a shot and run python train.py!
< 
< Let me know:
< - What the new Vocabulary Size number is once it runs successfully.
< - If any other errors pop up further down the script.

> Yes it is working now thank you

< Awesome! Getting that first clean run with the tensors shifting and matching up perfectly is a massive milestone.
<
< Your train.py file is now officially reading raw data, mapping the alphabet, converting everything into deep-learning math arrays, and splitting the data into safe zones.
<
< Now that the file is running smoothly, let's jump back to where we left off. I highly recommend building the Batch Generator next.
<
< Right now, your code only looks at one single string of text at a time.
< Real AI systems are highly parallel—they look at multiple independent chunks of text simultaneously to maximize the speed of the graphics card (GPU).
< We need to write a clean function that randomly grabs a handful of chunks whenever the AI asks for a fresh batch of data.
<
< Step 1: Update your train.py
< |
< Let's introduce two new standard concepts:
< - batch_size: How many independent text sequences we want to process at the exact same time (we will set this to 4).
< - get_batch function: A utility that picks random starting spots in our data, pulls out the input (X) and target (Y) chunks, and stacks them together into neat rectangular matrices.
<
< Add this code to the absolute bottom of your train.py file:
< ```python
# ==========================================
# 9. THE BATCH GENERATOR
# ==========================================
# Number of independent text sequences to process in parallel
batch_size = 4 

def get_batch(split):
    # Select whether we are pulling from the training or validation set
    data = train_data if split == 'train' else val_data
    
    # Generate random starting points in the data array
    # We subtract block_size so we don't accidentally overflow past the end of the text
    ix = torch.randint(len(data) - block_size, (batch_size,))
    
    # Stack the random inputs and targets into matrices
    x_batch = torch.stack([data[i:i+block_size] for i in ix])
    y_batch = torch.stack([data[i+1:i+block_size+1] for i in ix])
    
    return x_batch, y_batch

# Grab a sample batch from our training split to test it
xb, yb = get_batch('train')

print("-" * 30)
print(f"Inputs Batch Shape (xb):  {xb.shape}  -> (batch_size, block_size)")
print(f"Targets Batch Shape (yb): {yb.shape}")
print("-" * 30)
print("Here is what the input batch matrix looks like inside:")
print(xb)
< ```
<
< What you will see when you run it:
< |
< When you run python train.py, the terminal will display a neat 4x8 grid of integers.
< 
< Instead of a single line of data, you have successfully generated a multi-dimensional matrix.
< Your future AI processor will read all 4 rows at the exact same fraction of a microsecond, speeding up training drastically!
<
< Where should we head next now that data feeding is completely solved?
< - Show me how to build the Embedding Layer (giving these flat integer IDs spatial meanings)
< - Show me how to set up the Bigram Language Model blueprint (our very first neural network class)

The "diff" in console output:
```myConsoleOutput
------------------------------
Inputs Batch Shape (xb):  torch.Size([4, 8])  -> (batch_size, block_size)
Targets Batch Shape (yb): torch.Size([4, 8])
------------------------------
Here is what the input batch matrix looks like inside:
tensor([[23, 25, 13,  1,  2, 17, 14, 20],
        [27, 17, 14,  1, 30, 23, 25, 13],
        [18, 26,  1, 18, 22,  1, 27, 17],
        [22,  1, 27, 17, 14, 25, 14,  1]])
```

(
  I'm gonna stop seeing further topics with the AI
  and just re-read the code / these notes / etc...

  Then I'll go back to seeing further topics with the AI after.
)
